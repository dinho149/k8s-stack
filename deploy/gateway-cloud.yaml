apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata: {name: dogfood}
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata: {name: platform, namespace: envoy-gateway-system}
spec:
  gatewayClassName: dogfood
  listeners:
    - name: https
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
        certificateRefs: [{name: platform-wildcard}]
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels: {dogfood.platform/routing: 'true'}
