{{- if .Values.portal.enabled }}
apiVersion: v1
kind: ConfigMap
metadata: {name: dogfood-backstage-config}
data:
  app-config.yaml: |
    {{- toYaml .Values.portal.config | nindent 4 }}
---
apiVersion: apps/v1
kind: Deployment
metadata: {name: dogfood-backstage}
spec:
  replicas: 1
  selector:
    matchLabels: {app: dogfood-backstage}
  template:
    metadata:
      labels: {app: dogfood-backstage}
    spec:
      automountServiceAccountToken: false
      securityContext: {runAsNonRoot: true, runAsUser: 10001, seccompProfile: {type: RuntimeDefault}}
      containers:
        - name: backend
          image: {{ .Values.portal.backendImage }}
          envFrom:
            - secretRef: {name: {{ .Values.secretName }}}
          env:
            - {name: NODE_ENV, value: production}
          ports: [{containerPort: 7007}]
          readinessProbe:
            httpGet: {path: /.backstage/health/v1/readiness, port: 7007}
          securityContext: {allowPrivilegeEscalation: false, capabilities: {drop: [ALL]}}
          resources:
            requests: {cpu: 100m, memory: 256Mi}
            limits: {memory: 1Gi}
          volumeMounts:
            - {name: config, mountPath: /config, readOnly: true}
      volumes:
        - name: config
          configMap: {name: dogfood-backstage-config}
---
apiVersion: v1
kind: Service
metadata: {name: dogfood-backstage}
spec:
  selector: {app: dogfood-backstage}
  ports: [{port: 7007, targetPort: 7007}]
---
apiVersion: apps/v1
kind: Deployment
metadata: {name: dogfood-portal}
spec:
  replicas: 1
  selector:
    matchLabels: {app: dogfood-portal}
  template:
    metadata:
      labels: {app: dogfood-portal}
    spec:
      automountServiceAccountToken: false
      containers:
        - name: frontend
          image: {{ .Values.portal.frontendImage }}
          ports: [{containerPort: 8080}]
          readinessProbe:
            httpGet: {path: /, port: 8080}
          securityContext: {allowPrivilegeEscalation: false, capabilities: {drop: [ALL]}}
          resources:
            requests: {cpu: 25m, memory: 32Mi}
            limits: {memory: 128Mi}
---
apiVersion: v1
kind: Service
metadata: {name: dogfood-portal}
spec:
  selector: {app: dogfood-portal}
  ports: [{port: 80, targetPort: 8080}]
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: {name: portal}
spec:
  parentRefs: [{name: platform, namespace: envoy-gateway-system}]
  hostnames: [{{ .Values.portal.hostname | quote }}]
  rules:
    - backendRefs: [{name: dogfood-portal, port: 80}]
{{- end }}
