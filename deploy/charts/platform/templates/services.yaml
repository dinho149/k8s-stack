apiVersion: v1
kind: ConfigMap
metadata: {name: dogfood-config}
data:
  platform.yaml: |
    {{- toYaml .Values.lifecycle.config | nindent 4 }}
---
apiVersion: v1
kind: ServiceAccount
metadata: {name: dogfood-lifecycle}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata: {name: dogfood-state}
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests: {storage: {{ .Values.lifecycle.storage }}}
---
apiVersion: apps/v1
kind: Deployment
metadata: {name: dogfood-lifecycle}
spec:
  replicas: 1
  strategy: {type: Recreate}
  selector:
    matchLabels: {app: dogfood-lifecycle}
  template:
    metadata:
      labels: {app: dogfood-lifecycle}
    spec:
      serviceAccountName: dogfood-lifecycle
      securityContext: {runAsNonRoot: true, runAsUser: 10001, fsGroup: 10001}
      containers:
        - name: lifecycle
          image: {{ .Values.lifecycle.image }}
          args: [serve, --config, /config/platform.yaml]
          securityContext: {allowPrivilegeEscalation: false, capabilities: {drop: [ALL]}}
          envFrom:
            - secretRef: {name: {{ .Values.secretName }}}
          ports: [{containerPort: 8088}]
          readinessProbe:
            httpGet: {path: /healthz, port: 8088}
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {memory: 512Mi}
          volumeMounts:
            - {name: config, mountPath: /config, readOnly: true}
            - {name: state, mountPath: /workspace/.dogfood}
      volumes:
        - name: config
          configMap: {name: dogfood-config}
        - name: state
          persistentVolumeClaim: {claimName: dogfood-state}
---
apiVersion: v1
kind: Service
metadata:
  name: dogfood-lifecycle
  labels: {app: dogfood-lifecycle}
spec:
  selector: {app: dogfood-lifecycle}
  ports: [{name: http, port: 8088, targetPort: 8088}]
{{- if .Values.agent.enabled }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dogfood-agent
  annotations:
    {{- toYaml .Values.agent.serviceAccountAnnotations | nindent 4 }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata: {name: dogfood-agent-sessions}
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests: {storage: {{ .Values.agent.storage }}}
---
apiVersion: apps/v1
kind: Deployment
metadata: {name: dogfood-agent}
spec:
  replicas: 1
  strategy: {type: Recreate}
  selector:
    matchLabels: {app: dogfood-agent}
  template:
    metadata:
      labels: {app: dogfood-agent}
    spec:
      serviceAccountName: dogfood-agent
      securityContext: {runAsNonRoot: true, runAsUser: 10001, fsGroup: 10001, seccompProfile: {type: RuntimeDefault}}
      containers:
        - name: agent
          image: {{ .Values.agent.image }}
          securityContext: {allowPrivilegeEscalation: false, capabilities: {drop: [ALL]}}
          envFrom:
            - secretRef: {name: {{ .Values.secretName }}}
          env:
            - {name: DOGFOOD_API_URL, value: 'http://dogfood-lifecycle:8088'}
            - {name: AGENT_STATE_DIR, value: /state}
            - {name: AGENT_PROVIDER, value: {{ .Values.agent.provider | quote }}}
            - {name: AGENT_CONCURRENCY, value: {{ .Values.agent.concurrency | quote }}}
            - {name: AWS_REGION, value: {{ .Values.agent.bedrock.region | quote }}}
            - {name: BEDROCK_MODEL, value: {{ .Values.agent.bedrock.model | quote }}}
            - {name: VERTEX_MODEL, value: {{ .Values.agent.vertex.model | quote }}}
            - {name: CLOUD_ML_REGION, value: {{ .Values.agent.vertex.region | quote }}}
            - {name: ANTHROPIC_VERTEX_PROJECT_ID, value: {{ .Values.agent.vertex.project | quote }}}
          ports: [{containerPort: 8090}]
          readinessProbe:
            httpGet: {path: /healthz, port: 8090}
          resources:
            requests: {cpu: 250m, memory: 512Mi}
            limits: {cpu: '2', memory: 2Gi}
          volumeMounts:
            - {name: sessions, mountPath: /state}
      volumes:
        - name: sessions
          persistentVolumeClaim: {claimName: dogfood-agent-sessions}
---
apiVersion: v1
kind: Service
metadata: {name: dogfood-agent}
spec:
  selector: {app: dogfood-agent}
  ports: [{port: 8090, targetPort: 8090}]
{{- end }}
